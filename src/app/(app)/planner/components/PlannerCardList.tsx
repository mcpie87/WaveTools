// import PlannerCard from "./PlannerCard";

// export default function PlannerCardList({ data }) {
//   if (!data) return <div>Loading...</div>;

//   return (
//     <div className="flex flex-row flex-wrap">
//       {data.map((item, index) => (
//         <PlannerCard key={index} index={index} item={item} />
//       ))}
//     </div>
//   );
// }